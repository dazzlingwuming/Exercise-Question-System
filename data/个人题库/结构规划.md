--- question ---

question_id: 1
module: Agent 基础与架构
type: single_choice
difficulty: 1
knowledge_points:
  - JSON Schema
  - required
stem: |
  在 JSON Schema 中，如果一个字段必须出现，应使用哪个关键字来指定？
options:
  A: required
  B: properties
  C: items
  D: dependencies
answer:
  - A
option_analysis:
  A: 正确 - `required` 用于定义必须出现的属性名称列表。
  B: 错误 - `properties` 用于定义对象的属性模式，而不是必需属性。
  C: 错误 - `items` 用于定义数组元素的模式。
  D: 错误 - `dependencies` 用于定义字段间的依赖关系，不用于指定必需属性。
interview_followups:
  - 如果没有指定 `required` 关键字，JSON Schema 对额外字段有何默认行为？
  - 在 Pydantic 模型中如何实现必需字段的效果？

--- question ---

question_id: 2
module: Agent 基础与架构
type: single_choice
difficulty: 2
knowledge_points:
  - Pydantic
  - 类型转换
stem: |
  Pydantic 的 BaseModel 默认会如何处理字段类型与实际输入值不匹配的情况？
options:
  A: 严格类型检查，不做任何类型转换
  B: 尝试自动类型转换，无法转换时抛出错误
  C: 将类型错误记录并返回 None
  D: 忽略该字段后继续验证
answer:
  - B
option_analysis:
  A: 错误 - Pydantic 默认会尝试将输入值转换为声明的类型。
  B: 正确 - Pydantic 会自动进行类型转换，如转换数字类型等，若转换失败则抛出验证错误。
  C: 错误 - Pydantic 不会默默返回 None，而是报告验证错误。
  D: 错误 - Pydantic 不会忽略字段不匹配，若类型无法转换则会直接抛错。
interview_followups:
  - 如何在 Pydantic 中禁用自动类型转换以提高安全性？
  - 给出一个 Pydantic 字段类型转换的示例及其效果。

--- question ---

question_id: 3
module: Agent 基础与架构
type: single_choice
difficulty: 2
knowledge_points:
  - FastAPI
  - response_model
stem: |
  在 FastAPI 中，如果返回的数据包含 response_model 未定义的字段，会发生什么？
options:
  A: 自动过滤掉这些字段
  B: 抛出验证错误
  C: 原样返回所有字段
  D: 用默认值替换这些字段
answer:
  - A
option_analysis:
  A: 正确 - FastAPI 会根据 `response_model` 定义自动过滤掉未在模型中定义的额外字段。
  B: 错误 - FastAPI 默认不会因为额外字段而抛错，只会过滤并返回符合模型的字段。
  C: 错误 - FastAPI 不会原样返回未定义字段，而是移除它们以符合 `response_model`。
  D: 错误 - FastAPI 不会为未定义字段提供默认值，而是直接丢弃。
interview_followups:
  - 如果希望保留额外字段，应如何配置 FastAPI？
  - FastAPI 的 response_model_include 和 response_model_exclude 有什么作用？

--- question ---

question_id: 4
module: Agent 基础与架构
type: single_choice
difficulty: 1
knowledge_points:
  - JSON Schema
  - 数组
stem: |
  以下哪个 JSON Schema 关键字用于指定数组元素的模式（schema）？
options:
  A: items
  B: properties
  C: additionalItems
  D: prefixItems
answer:
  - A
option_analysis:
  A: 正确 - `items` 用于定义数组中每个元素应符合的模式。
  B: 错误 - `properties` 用于定义对象的属性模式。
  C: 错误 - `additionalItems` 用于定义元组样式数组中额外项的模式（旧版）。
  D: 错误 - `prefixItems` 是 JSON Schema 2020-12 用于元组前缀项，`prefixItems` 在较老版本中称为 `items`。
interview_followups:
  - JSON Schema 中，如何针对固定长度的数组定义每个位置不同的模式？
  - 在 JSON Schema 2020-12 规范中，`prefixItems` 与 `items` 有何区别？

--- question ---

question_id: 5
module: Agent 基础与架构
type: single_choice
difficulty: 2
knowledge_points:
  - Pydantic
  - Field
stem: |
  在 Pydantic 模型中，使用哪个参数可以为字段指定不同于属性名的序列化名称（字段别名）？
options:
  A: alias
  B: title
  C: description
  D: default
answer:
  - A
option_analysis:
  A: 正确 - `alias` 参数用于在序列化时指定字段的别名。
  B: 错误 - `title` 仅用于文档标题，不会改变字段名称。
  C: 错误 - `description` 用于文档描述，不用于别名。
  D: 错误 - `default` 指定默认值，不是字段别名。
interview_followups:
  - 如何在 Pydantic 的全局配置中设置默认的字段别名生成策略？
  - 当使用了 `alias` 时，如何控制模型输出时是否使用别名？

--- question ---

question_id: 6
module: Agent 基础与架构
type: single_choice
difficulty: 2
knowledge_points:
  - JSON Schema
  - additionalProperties
stem: |
  在 JSON Schema 中，如果未显式指定 `additionalProperties`，默认情况下是否允许对象包含未定义的额外属性？
options:
  A: 允许
  B: 不允许
  C: 取决于 JSON Schema 版本
  D: 只对数组有效
answer:
  - A
option_analysis:
  A: 正确 - JSON Schema 默认允许额外属性，即 `additionalProperties: true`。
  B: 错误 - 只有当 `additionalProperties` 显式设为 `false` 时，才不允许额外属性。
  C: 错误 - 各版本默认行为一致，均允许额外属性。
  D: 错误 - `additionalProperties` 仅适用于对象，与数组无关。
interview_followups:
  - 如果想在 JSON Schema 中禁止额外属性，应如何设置 `additionalProperties`？
  - FastAPI 中，是否可以全局配置忽略额外字段还是抛出错误？

--- question ---

question_id: 7
module: Agent 基础与架构
type: multiple_choice
difficulty: 2
knowledge_points:
  - JSON Schema
  - 数值约束
stem: |
  以下哪些关键字可以在 JSON Schema 中用于约束数值的范围？（可多选）
options:
  A: minimum
  B: maximum
  C: pattern
  D: exclusiveMinimum
answer:
  - A
  - B
  - D
option_analysis:
  A: 正确 - `minimum` 定义数值的下限。
  B: 正确 - `maximum` 定义数值的上限。
  C: 错误 - `pattern` 用于字符串的正则约束。
  D: 正确 - `exclusiveMinimum` 定义严格下限（小于等于不被接受）。
interview_followups:
  - JSON Schema 中如何表示一个数值必须是 5 的倍数？
  - 如果需要同时设置上限和下限，该如何在 Pydantic 模型中定义？

--- question ---

question_id: 8
module: Agent 基础与架构
type: multiple_choice
difficulty: 2
knowledge_points:
  - Pydantic
  - Field
stem: |
  以下哪些参数可以在 Pydantic 的 `Field()` 中用于定义字段的验证约束？（可多选）
options:
  A: gt
  B: regex
  C: alias
  D: min_length
answer:
  - A
  - B
  - D
option_analysis:
  A: 正确 - `gt` 表示数值必须大于指定值。
  B: 正确 - `regex`（或在 schema 中称作 `pattern`）用于定义字符串的正则表达式约束。
  C: 错误 - `alias` 用于指定字段别名，不用于验证。
  D: 正确 - `min_length` 定义字符串的最小长度约束。
interview_followups:
  - Pydantic Field 中常用的字符串约束参数还有哪些？
  - 如何在 Field 中同时设置值的范围（例如大于 0 且小于 100）？

--- question ---

question_id: 9
module: Agent 基础与架构
type: multiple_choice
difficulty: 2
knowledge_points:
  - FastAPI
  - response_model
stem: |
  以下哪些说法关于 FastAPI 的 `response_model` 参数是正确的？（可多选）
options:
  A: 用于生成自动文档和 OpenAPI 模式
  B: 在返回之前验证并过滤输出数据
  C: 自动将 ORM 或字典转换为 Pydantic 对象
  D: 会影响请求体的验证过程
answer:
  - A
  - B
  - C
option_analysis:
  A: 正确 - `response_model` 用于生成 API 文档和定义响应的 JSON Schema。
  B: 正确 - FastAPI 会在发送响应前使用 `response_model` 进行数据验证和过滤。
  C: 正确 - 返回 ORM 实例或字典时，FastAPI 会自动使用 Pydantic 将其序列化为 JSON 对象。
  D: 错误 - `response_model` 仅用于响应，与请求验证无关。
interview_followups:
  - 如果不想让 FastAPI 过滤字段，应该如何设置 `response_model`？
  - `response_model_include` 和 `response_model_exclude` 有什么用途？

--- question ---

question_id: 10
module: Agent 基础与架构
type: multiple_choice
difficulty: 2
knowledge_points:
  - JSON Schema
  - 组合模式
stem: |
  以下哪些是 JSON Schema 中用于组合子模式的关键字？（可多选）
options:
  A: allOf
  B: oneOf
  C: properties
  D: anyOf
answer:
  - A
  - B
  - D
option_analysis:
  A: 正确 - `allOf` 要求数据符合所有子模式。
  B: 正确 - `oneOf` 要求数据符合其中一个子模式。
  C: 错误 - `properties` 用于对象的属性定义，而非模式组合。
  D: 正确 - `anyOf` 要求数据符合任一子模式。
interview_followups:
  - `allOf`、`anyOf` 和 `oneOf` 在使用上有什么区别？
  - 在 Pydantic 模型中如何表示联合（union）类型以产生类似的效果？

--- question ---

question_id: 11
module: Agent 基础与架构
type: multiple_choice
difficulty: 2
knowledge_points:
  - Pydantic
  - BaseModel
stem: |
  以下哪些属于 Pydantic `BaseModel` 的特性？（可多选）
options:
  A: 自动验证字段类型
  B: 自动生成 JSON Schema
  C: 默认忽略未定义的额外字段
  D: 自动将所有字段转换为字符串
answer:
  - A
  - B
  - C
option_analysis:
  A: 正确 - Pydantic 在实例化模型时会自动验证字段类型。
  B: 正确 - Pydantic 支持通过 `model_json_schema()` 等方法生成 JSON 模式。
  C: 正确 - 默认情况下，额外字段（未在模型中定义）会被忽略而不是报错。
  D: 错误 - Pydantic 不会自动把所有字段转为字符串，只会尝试进行类型转换。
interview_followups:
  - 如何配置 `BaseModel` 让额外字段出现时报错而不是忽略？
  - Pydantic 自动生成的 JSON Schema 有哪些用途？

--- question ---

question_id: 12
module: Agent 基础与架构
type: multiple_choice
difficulty: 3
knowledge_points:
  - 向后兼容
  - 版本控制
stem: |
  以下哪些做法有助于维护 JSON Schema 或 Pydantic 模型的向后兼容性？（可多选）
options:
  A: 为新增字段提供默认值
  B: 删除旧版本的字段
  C: 为旧字段添加别名（alias）
  D: 将新增字段设为可选
answer:
  - A
  - D
option_analysis:
  A: 正确 - 给新增字段设置默认值，可确保旧数据兼容且不会因缺失该字段而失败。
  B: 错误 - 删除旧字段会导致旧版本客户端无法解析新模式。
  C: 错误 - 给字段添加别名可能会引起混淆，通常不作为主要兼容方案。
  D: 正确 - 将新增字段设为可选（Optional）使得旧数据缺少该字段时仍能通过验证。
interview_followups:
  - 为什么在 API 版本升级中，一般建议为新字段设置默认值而非直接强制要求？
  - 在 Pydantic 中，如何使用 `alias` 或 `Field(default=...)` 来保持兼容性？

--- question ---

question_id: 13
module: Agent 基础与架构
type: true_false
difficulty: 2
knowledge_points:
  - JSON Schema
  - required
stem: |
  在 JSON Schema 中，`required` 关键字后面应跟一个属性名称的字符串列表，指定哪些属性是必需的。
options:
  A: 正确
  B: 错误
answer:
  - A
option_analysis:
  A: 正确 - `required` 后面是一个字符串数组，用于列出所有必须存在的属性。
  B: 错误 - 说明与实际不符。
interview_followups:
  - 如果在验证时发现缺少必需属性，应该如何报告错误？
  - Pydantic 模型中未声明字段是否会被自动视为必需？

--- question ---

question_id: 14
module: Agent 基础与架构
type: true_false
difficulty: 3
knowledge_points:
  - Pydantic
  - extra
stem: |
  默认情况下，Pydantic 在 BaseModel 中会忽略输入数据中未定义的额外字段，而不会抛出错误。
options:
  A: 正确
  B: 错误
answer:
  - A
option_analysis:
  A: 正确 - 默认配置下，Pydantic 的 `extra` 行为为 `ignore`，会忽略模型中未定义的额外字段。
  B: 错误 - 说明与默认行为不符。
interview_followups:
  - 如何修改 Pydantic 配置以在遇到额外字段时抛出错误？
  - 在使用 FastAPI 时，额外字段如何影响输入验证和输出？

--- question ---

question_id: 15
module: Agent 基础与架构
type: true_false
difficulty: 3
knowledge_points:
  - LLM
  - 结构化输出
stem: |
  使用 OpenAI 的结构化输出模式（`response_format=json_schema`）时，模型会严格遵循给定的 JSON Schema 来生成输出。
options:
  A: 正确
  B: 错误
answer:
  - A
option_analysis:
  A: 正确 - 结构化输出功能通过约束解码过程，保证模型输出符合 JSON 模式。
  B: 错误 - 与功能说明不符。
interview_followups:
  - 哪些 OpenAI 模型支持结构化输出功能？
  - 结构化输出相比传统 JSON 模式有何优势？

--- question ---

question_id: 16
module: Agent 基础与架构
type: concept_compare
difficulty: 2
knowledge_points:
  - JSON Schema
  - Pydantic
stem: |
  比较 JSON Schema 和 Pydantic 模型 在定义数据结构和验证方面的异同。
reference_answer: |
  JSON Schema 是一种声明式的数据结构描述语言，通过 JSON 格式定义字段类型、约束、结构等，可以用于多种语言和工具的数据验证和文档生成。Pydantic 模型则是基于 Python 的类（继承 BaseModel），使用类型注解来定义数据结构，并在运行时进行数据验证和自动类型转换。主要区别包括：  
  1. **定义方式**：JSON Schema 使用 JSON 文件结构声明字段和约束；Pydantic 使用 Python 类和字段类型注解。  
  2. **验证时机**：JSON Schema 通常在独立验证工具中使用，Pydantic 则在模型实例化时即时验证数据。  
  3. **可移植性**：JSON Schema 是跨语言标准，可用于前后端共享；Pydantic 专用于 Python 环境。  
  4. **特性支持**：Pydantic 模型可指定默认值、别名、嵌套模型等，并自动生成 JSON Schema；JSON Schema 可定义模式组合（allOf/oneOf）和格式（format）等。  
  5. **使用场景**：FastAPI 中使用 Pydantic 来定义请求/响应模型；在需要生成文档或与其他系统交互时，可使用 JSON Schema。  
answer_keywords:
  - 声明式 vs Python 类
  - 运行时验证 vs 离线验证
  - 可移植性 vs Python依赖
  - 嵌套 模型
  - 默认值 别名
scoring_points:
  - JSON Schema 通过 JSON 声明数据结构和约束；Pydantic 通过 Python 类和类型注解定义并验证数据。  
  - JSON Schema 语言无关，可生成文档；Pydantic 专为 Python，可自动生成 JSON Schema。  
  - Pydantic 在实例化时实时验证并转换类型；使用 JSON Schema 需要借助验证器工具。  
  - Pydantic 支持默认值、嵌套模型、别名等高级功能；JSON Schema 需要手动定义嵌套和组合模式。  
  - 在 FastAPI 中通常使用 Pydantic 模型作为请求/响应模式；JSON Schema 适用于与多语言系统的数据交换。  
interview_followups:
  - 在什么情况下你会选择使用 JSON Schema 而不是 Pydantic 模型？
  - 如何将一个 Pydantic 模型转换为 JSON Schema？

--- question ---

question_id: 17
module: Agent 基础与架构
type: concept_compare
difficulty: 3
knowledge_points:
  - Pydantic
  - 校验器
stem: |
  比较 Pydantic 模型中的 `@validator` 装饰器 和字段级 `Field()` 验证参数 在数据校验中的作用和区别。
reference_answer: |
  在 Pydantic 模型中，`Field()` 的参数（如 gt、lt、regex 等）是在模型定义时静态指定的，用于基本约束和自动生成 JSON Schema；而 `@validator` 装饰的方法可编写自定义逻辑进行验证。它们的区别在于：  
  - `Field()` 参数：用于单字段的简单约束（数值范围、字符串格式等），由 Pydantic 底层自动应用并体现在 JSON Schema 中。  
  - `@validator`：可编写任意复杂的验证函数，能够访问字段值并抛出错误，适用于需要跨字段或复杂规则的情况。  
  - 执行顺序：先执行类型转换和 `Field()` 约束，再执行 `@validator` 方法。  
  - 示例：对字段做正则检查可使用 `regex` 参数，而计算两个字段之和等逻辑需要用 `@validator`。  
answer_keywords:
  - Field 验证参数
  - 自定义校验
  - 静态 vs 动态
  - 执行顺序
  - JSON Schema 生成
scoring_points:
  - `Field()` 参数是在定义时声明的约束（如范围、长度、格式），自动生效并生成 JSON 模式。  
  - `@validator` 用于编写自定义逻辑，可以进行更复杂的验证或依赖其他字段。  
  - `Field()` 约束只能处理单字段基本验证；`@validator` 可访问多个字段并执行任意验证。  
  - Pydantic 首先应用类型转换和 `Field()` 约束，再运行 `@validator` 定义的函数。  
  - 使用场景区分：简单类型或格式约束可使用 `Field()` 参数；复杂逻辑或关联校验需要 `@validator`。  
interview_followups:
  - 如何让一个 `@validator` 方法只在模型创建时执行，而在更新操作时跳过？
  - Pydantic v2 中的 `field_validator` 和 `model_validator` 有何不同？

--- question ---

question_id: 18
module: Agent 基础与架构
type: concept_compare
difficulty: 2
knowledge_points:
  - FastAPI
  - response_model
stem: |
  比较 FastAPI 路由函数的返回类型注释（Return Type）与 `response_model` 参数 在功能上的异同。
reference_answer: |
  FastAPI 中，返回类型注释主要用于代码静态检查和文档的可读性，而 `response_model` 参数则直接影响运行时行为。主要区别如下：  
  - **用途不同**：返回类型注释供开发者和工具（如 IDE、MyPy）使用，不会主动触发 FastAPI 的验证；而 `response_model` 告诉 FastAPI 使用指定模型来验证和过滤响应，并生成 OpenAPI 文档。  
  - **优先级**：如果同时指定返回类型和 `response_model`，FastAPI 优先使用 `response_model` 进行验证和输出控制。  
  - **适用场景**：当函数返回值类型本身就与注释匹配时，可以只用注释；当需要返回非注释类型（如 ORM 对象）或过滤数据时，使用 `response_model`。  
  - **功能影响**：`response_model` 会强制返回的数据符合模型定义（缺失字段报错、多余字段过滤），还会自动生成响应的 JSON 模式；返回类型注释不具备这些功能。  
answer_keywords:
  - 返回类型 注释
  - response_model 参数
  - 验证 过滤
  - 文档 生成
  - 优先级
scoring_points:
  - 返回类型注释是 Python 类型提示，用于静态检查和可读性，不会自动触发 FastAPI 验证或过滤。  
  - `response_model` 参数会启用 Pydantic 验证，保证实际响应符合模型定义，还用于生成 OpenAPI 文档。  
  - 当同时指定两者时，FastAPI 使用 `response_model` 而忽略返回注释的类型。  
  - 不使用 `response_model` 时，返回类型注释不会阻止返回额外字段；使用 `response_model` 时额外字段将被移除。  
  - 在返回值与注释类型不一致时，可通过 `response_model` 指定正确的模型以保证接口正确性。  
interview_followups:
  - 如何在 FastAPI 中返回一个自定义对象并仍然生成正确的文档？
  - 如果只指定返回注释而没有 `response_model`，FastAPI 会如何处理响应？

--- question ---

question_id: 19
module: Agent 基础与架构
type: concept_compare
difficulty: 3
knowledge_points:
  - LLM
  - 函数调用
stem: |
  比较 OpenAI 的函数调用（Function Calling）和结构化输出（`response_format = json_schema`）在使用场景和机制上的异同。
reference_answer: |
  OpenAI 的函数调用和结构化输出都是为了获得结构化数据，但使用方式和目标不同：  
  - **函数调用**：为 LLM 提供一个或多个函数定义（包括参数的 JSON Schema），模型通过输出调用相应函数来与外部功能集成。它适用于需要执行实际操作（如查询数据库、调用 API）的场景。模型会输出函数名和参数，而不是直接返回最终结果。  
  - **结构化输出（json_schema 模式）**：直接给模型一个 JSON Schema 作答格式要求，模型按照该模式生成完整的 JSON 响应，而不调用任何实际函数。适合需要严格按照给定格式呈现回答的场景，如生成报告或 UI 渲染数据。  
  - **区别**：函数调用会触发实际函数执行，模型的回答过程被分为“调用函数”和“返回值”；结构化输出则是一次性输出符合模式的结果，不涉及外部调用。前者适合工具交互，后者适合直接生成结构化内容。  
  - **示例**：如果要查询天气信息，可以使用函数调用并在函数中实现查询逻辑；如果要生成符合前端展示格式的 JSON 数据，则用结构化输出让模型直接输出 JSON。  
answer_keywords:
  - Function Calling
  - response_format
  - 工具 集成
  - 输出格式
  - JSON Schema
scoring_points:
  - 函数调用：向模型提供函数签名，模型输出函数名和参数，由系统执行该函数。  
  - 结构化输出：向模型提供 JSON Schema，要模型直接输出符合该模式的 JSON 对象。  
  - 使用场景：函数调用用于需要调用后端服务、工具的场景；结构化输出用于需要规范回答格式但不需要外部调用的场景。  
  - 机制：函数调用返回值可能作为下一步输入；结构化输出是模型的最终回答，确保响应直接符合模式。  
  - 兼容性：不同模型支持的方式可能不同，需要根据模型版本选择方案。  
interview_followups:
  - 如何在 OpenAI API 中同时使用函数调用和结构化输出？
  - 结构化输出在模型不支持的情况下，有什么替代方案？

--- question ---

question_id: 20
module: Agent 基础与架构
type: scenario_debug
difficulty: 3
knowledge_points:
  - FastAPI
  - Pydantic
stem: |
  在使用 FastAPI 时，定义了如下 Pydantic 模型作为响应模型，并在路由中应用：  
  ```python
  from pydantic import BaseModel

  class DataModel(BaseModel):
      name: str
      value: int
      id: int

  @app.get("/data/", response_model=DataModel)
  async def get_data():
      return {"name": "Alice", "value": 100}
  ```  
  运行时返回错误：`ValidationError: field "id" required`。请分析出现错误的原因并给出优化方案。
reference_answer: |
  错误原因是定义的 `DataModel` 包含必需字段 `id`，但路由函数返回的数据中缺少 `id` 字段，导致 Pydantic 验证失败。  
  **诊断步骤**：检查错误信息发现缺少 `id`；核对模型与返回数据，发现返回的字典缺少必需的 `id`。FastAPI 根据 `response_model` 强制验证并发现字段缺失。  
  **优化方案**：可考虑以下方式优化：  
  - 在返回数据中增加 `id` 值，使其符合模型要求。  
  - 如果 `id` 非必需，可将其设为可选（`id: int | None = None`），或给 `id` 设置默认值。  
  - 使用 `response_model_exclude_unset=True` 参数，仅返回实际存在的字段。  
answer_keywords:
  - 缺少必需字段
  - 默认值
  - 可选字段 (Optional)
  - FastAPI 验证
scoring_points:
  - 识别错误信息：`ValidationError` 提示缺少必需字段 `id`。  
  - 找出差异：模型定义要求 `id`，但返回数据中未提供该字段。  
  - FastAPI 会在返回前使用 `response_model` 验证数据，确保符合模型定义。  
  - 优化：在返回的数据中包含 `id`，或在模型中给 `id` 设置默认值/可选类型。  
  - 或者使用 FastAPI 的 `response_model_exclude_unset` 等参数，只验证并返回实际提供的字段。  
diagnosis_steps:
  - 查看错误信息，发现缺少 `id` 字段导致验证失败。  
  - 检查 `DataModel` 模型定义和实际返回的数据格式，确认 `id` 是必需但未提供。  
  - 确认 FastAPI 将返回值与模型匹配，如果缺失必需字段则抛出错误。
optimization_points:
  - 在返回的字典中增加 `id` 键及其值，使返回数据满足模型要求。  
  - 修改模型，将 `id: int` 改为可选类型（如 `id: int | None = None`）或提供默认值，以兼容缺失字段。  
  - 或使用 `@app.get(..., response_model_exclude_unset=True)` 等配置，让 FastAPI 只关注实际返回的字段。

--- question ---

question_id: 21
module: Agent 基础与架构
type: scenario_debug
difficulty: 3
knowledge_points:
  - Pydantic
  - 类型不匹配
stem: |
  有以下 Pydantic 模型定义用于结构化输出：  
  ```python
  class Event(BaseModel):
      name: str
      date: str
      participants: list[str]
  ```  
  当 Agent 运行返回结果为 `{"name": "ChatGPT Summit", "date": 20231201, "participants": ["Alice", "Bob"]}` 时，出现验证错误。请定位问题并提出解决方法。
reference_answer: |
  错误原因是 `date` 字段的值类型不匹配：模型定义中 `date` 为字符串类型，返回结果中却是整数（20231201），导致 Pydantic 验证失败。  
  **定位步骤**：查看错误信息，发现是 `date` 的类型错误；检查模型字段类型与输出值类型的差异，发现不一致。  
  **优化方案**：确保输出符合模型定义或调整模型：  
  - 在 Agent 输出中将日期转换为字符串格式，例如 `"2023-12-01"`。  
  - 或修改模型，将 `date` 字段定义为可接受整数的类型（如 `date: int` 或使用 `datetime`）。  
  - 可以在模型中添加校验器，将整数转换为字符串或反之。
answer_keywords:
  - 类型不匹配
  - 转换 类型
  - 日期 格式
scoring_points:
  - 确认错误：`date` 字段期望字符串，但收到整数，触发类型验证失败。  
  - 分析模型：检查 `Event` 中 `date` 的类型声明，与实际输出不符。  
  - 优化：修改输出使 `date` 为字符串，或修改模型类型（如改为 `int` 或 `datetime`），确保类型匹配。  
  - 也可增加自定义校验，将数字转为字符串或日期对象。  
diagnosis_steps:
  - 根据错误信息定位到 `date` 字段验证失败。  
  - 检查模型定义发现 `date: str`，与返回的 `20231201`（整数）类型不一致。  
  - 确认 Pydantic 会严格检查类型，因此必须让输出类型和模型类型一致。  
optimization_points:
  - 在 Agent 的输出中将 `date` 值格式化为字符串（如 `"2023-12-01"`）。  
  - 或在模型中调整 `date` 类型（改为 `int` 或 `datetime`），使其能接受当前输出。  
  - 可使用 Pydantic 校验器 (`@validator`) 来自动转换或验证 `date` 值，处理不同类型。

--- question ---

question_id: 22
module: Agent 基础与架构
type: scenario_debug
difficulty: 3
knowledge_points:
  - Pydantic
  - 格式校验
stem: |
  考虑以下 Pydantic 模型：  
  ```python
  class User(BaseModel):
      email: EmailStr
  ```  
  如果模型运行结果为 `{"email": "notanemail"}`，会导致验证失败。请分析原因并提出可能的优化方案。
reference_answer: |
  错误原因是 `EmailStr` 类型要求字段值必须是有效的邮箱格式，但 `"notanemail"` 不是合法的邮箱地址，导致验证失败。  
  **定位步骤**：根据异常信息确认是邮箱格式错误；核对模型发现使用了 `EmailStr`，其内部会验证格式。  
  **优化方案**：  
  - 指导模型输出有效的邮箱格式，或在提示中明确要求提供正确格式的邮箱。  
  - 如果邮箱格式验证过于严格，可将字段类型改为普通 `str`，或自定义验证器来放宽规则。  
  - 使用 `Field(regex=...)` 等参数定义自定义格式校验。
answer_keywords:
  - EmailStr
  - 格式验证
  - 自定义校验
scoring_points:
  - 识别问题：使用了 `EmailStr` 使得模型验证邮箱格式，输出 `"notanemail"` 不符合格式要求。  
  - 分析模型：`EmailStr` 是 Pydantic 的邮箱字符串类型，会严格检查格式。  
  - 优化：提示模型生成合法邮箱地址；或改用普通 `str` 类型；也可编写 `regex` 或 `@validator` 实现更宽松的验证。  
diagnosis_steps:
  - 查看错误信息，发现字段 `email` 格式不正确。  
  - 检查模型定义，发现 `email` 使用 `EmailStr` 类型，因此输入必须是有效邮箱地址。  
  - 确认异常原因：未提供符合 `EmailStr` 格式的字符串。  
optimization_points:
  - 改进模型：将 `email` 类型改为 `str`，或设置更宽松的正则表达式，让任意字符串都可接受。  
  - 改进输出：让 LLM 生成合法邮箱或提供默认值。  
  - 或在模型中使用自定义验证器，给出格式说明或自动修正输入（如自动补全缺失的“@domain”部分）。

--- question ---

question_id: 23
module: Agent 基础与架构
type: scenario_debug
difficulty: 4
knowledge_points:
  - JSON 格式
  - LLM
stem: |
  Pydantic 模型定义为：  
  ```python
  class Product(BaseModel):
      name: str
      price: float
  ```  
  但是 Agent 返回的 JSON 为 `{name: "Apple", price: 1.2}`（字段名未加双引号），导致 JSON 解析失败。请分析问题并提出解决办法。
reference_answer: |
  错误原因是返回的格式不是有效的 JSON：字段名 `name` 没有用双引号包裹，违背了 JSON 语法规则。  
  **定位步骤**：JSON 解析器报错位置指示无效格式，检查发现字段名缺少引号。  
  **优化方案**：确保模型输出满足 JSON 语法：  
  - 在提示或系统指令中明确要求使用严格的 JSON 格式，包括为键添加引号。  
  - 使用 OpenAI 的结构化输出功能（如 `response_format=json_schema`），让模型自动输出规范的 JSON。  
  - 对输出进行后处理，在最终解析前修复语法错误（如添加引号）。
answer_keywords:
  - JSON 语法
  - 字段名 引号
  - 结构化 输出
scoring_points:
  - 识别问题：返回的内容缺少引号，不符合 JSON 标准，导致解析失败。  
  - 分析：检查输出格式，确认应该是 `"name": "Apple"` 而不是 `{name: ...}`。  
  - 优化：指导模型生成合法 JSON，如在提示中要求输出 JSON 代码块或使用 `response_format` 强制格式化。  
  - 另外，可在应用层对结果做简单字符串修正，确保最终 JSON 能被解析。  
diagnosis_steps:
  - JSON 解析报错，发现是由于字段名未用双引号引起。  
  - 检查 Pydantic 模型和期望格式，确认输出应为 JSON 字符串。  
  - 确认模型输出没有按照 JSON 规则输出键名。  
optimization_points:
  - 提示模型严格按照 JSON 格式输出，例如在系统提示中强调使用双引号括键。  
  - 使用 OpenAI 结构化输出（`json_schema` 或函数调用）强制生成符合模式的 JSON。  
  - 在接收输出后，可以进行后处理（如正则）来补全缺失的引号以构成合法 JSON。

--- question ---

question_id: 24
module: Agent 基础与架构
type: scenario_debug
difficulty: 3
knowledge_points:
  - Pydantic
  - Union
stem: |
  有以下 Pydantic 模型：  
  ```python
  class Response(BaseModel):
      result: int | str
  ```  
  如果 Agent 输出为 `{"result": true}`（布尔类型），则解析失败。请找出原因并提出解决方案。
reference_answer: |
  错误原因是模型定义中 `result` 类型限定为 `int` 或 `str`，而输出中的 `true` 是布尔类型，既不属于 `int` 也不属于 `str`，导致验证失败。  
  **定位步骤**：查看异常信息可发现 `true` 不是合法的 `result` 值；检查模型类型，发现未包含 `bool`。  
  **优化方案**：  
  - 如果允许布尔值，将模型类型改为包含 `bool`，如 `result: int | str | bool`。  
  - 如果 `result` 本该是数字，可以在模型中使用 `bool` 可被视为 `int` 的配置，或添加一个校验器将布尔转换为对应值。  
  - 修改提示语让模型输出整数而非布尔值。
answer_keywords:
  - 类型不匹配
  - Union
  - 布尔 非法
scoring_points:
  - 识别问题：模型只接受 `int` 或 `str`，但输出中有布尔类型 `true`，不符合 Union 类型要求。  
  - 分析：确认 `bool` 不是 `int` 或 `str` 的子类型（尽管在 Python 中 `bool` 是 `int` 的子类，但 Pydantic 默认不允许布尔匹配整数）。  
  - 优化：将类型声明改为 `int | str | bool`，或在模型中加入校验逻辑，将 `true` 视为某个数值。  
  - 另一种方法：调整提示，让模型输出符合当前类型要求的结果（如将 `true` 改为 1）。  
diagnosis_steps:
  - 查看验证错误，发现 `true` 类型不匹配预期。  
  - 检查模型定义，确认 `result` 的类型为 `int | str`，未包含 `bool`。  
  - 确认 Pydantic 验证 Union 时，不会将布尔自动视为整数。  
optimization_points:
  - 修改模型：将 `result` 类型扩展为包括 `bool`，使其兼容布尔输出。  
  - 或添加自定义校验，将 `true` 转换为对应的整数值。  
  - 在提示中避免让模型输出布尔，将其输出为整数或字符串表示形式。

--- question ---

question_id: 25
module: Agent 基础与架构
type: system_design
difficulty: 4
knowledge_points:
  - 架构
  - 验证
stem: |
  设计一个用于确保大型语言模型输出符合 JSON Schema 验证要求的系统架构。
reference_answer: |
  **系统核心模块**：用户请求处理模块、LLM 模型接口模块、验证模块（使用 Pydantic 或 JSON Schema 验证器）、错误处理模块和日志监控模块。  
  **数据流**：用户发起请求 → 系统调用 LLM 生成输出（JSON 格式）→ 将输出传递给验证模块（Pydantic 模型或独立 JSON Schema 验证）→ 如果验证通过则返回结果，否则记录错误并根据策略（重试或返回错误提示）处理。  
  **评估指标**：验证成功率（符合模式的输出比例）、响应延迟（端到端的总时延）、系统吞吐量（每秒请求数）、错误率等。  
  **风险点**：模型输出可能不符合模式（如字段缺失、类型错误、无效 JSON），需要健壮的错误捕获；验证模块的性能瓶颈可能影响实时性；模式更新需要同步所有组件，避免版本不一致；安全方面需防范注入攻击或恶意输出。  
answer_keywords:
  - 验证模块
  - 数据流
  - LLM 接口
  - 性能监控
  - 安全边界
scoring_points:
  - 明确系统的核心模块，如模型调用层、输出验证层、错误处理、日志监控等。  
  - 数据流：描述请求到模型再到验证的流程，例如用户请求→LLM生成JSON→验证器检验→返回或重试。  
  - 验证模块可以基于 Pydantic 或 JSON Schema，自行实现或调用库进行检查。  
  - 评估指标：包括模型输出准确度（正确解析的比例）、验证成功率、响应时间、吞吐量和错误率等。  
  - 风险点：输出格式不符导致验证失败、系统性能开销（验证延迟）、模型恶意或错误输出需降级、Schema 版本不一致等。  
architecture_points:
  - 用户接口层、LLM 调用层、JSON Schema/Pydantic 验证层、结果处理层和监控日志组件。  
  - 验证层部署多个实例以扩展并发处理能力，可使用缓存和异步调用优化性能。  
data_flow:
  - 请求到达 → 预处理 → LLM 生成结构化输出 → 传递给验证层 → 验证通过→ 返回结果；若失败→错误处理（提示用户或降级）。  
evaluation_metrics:
  - 模型输出的验证通过率（匹配 schema 的百分比）、请求成功率、平均响应时间、系统吞吐量、资源利用率等。  
risk_points:
  - 模型输出不符合模式（字段缺失或类型错误）导致解析失败；  
  - 验证过程可能成为瓶颈，带来延迟；  
  - 模式/模型更新导致兼容性问题；  
  - 潜在安全问题，如恶意输入绕过验证、注入风险。

--- question ---

question_id: 26
module: Agent 基础与架构
type: system_design
difficulty: 4
knowledge_points:
  - 版本控制
  - 向后兼容
stem: |
  设计一个多版本兼容的系统架构，使得 Pydantic 模型和 JSON Schema 在版本迭代时保持对旧客户端的兼容性。
reference_answer: |
  **核心组件**：API 网关（版本路由）、模型服务（维护多个版本的 Pydantic 模型或 Schema）、转换适配层和客户端兼容层。  
  **数据流**：客户端请求带有版本号 → API 网关根据版本号路由到相应的服务版本 → 服务使用对应版本的 Pydantic 模型验证输入/输出 → 如果请求或数据是旧格式，适配层负责字段映射或填充默认值 → 返回数据也根据客户端版本格式化。  
  **评估指标**：旧版本兼容请求比例、新老版本切换失败率、升级影响范围（如需要回滚次数）、验证错误率等。  
  **风险点**：模式变更导致旧版本客户端数据无法解析、数据混用导致冲突；维护多版本模型增加复杂度；版本管理策略不当可能引发安全或数据错误。  
answer_keywords:
  - API 网关
  - 多版本模型
  - 字段映射
  - 兼容层
  - 回归测试
scoring_points:
  - 核心模块：包括版本路由层、后端服务层（可部署多个版本的模型逻辑）、适配转换层（处理新旧字段差异）等。  
  - 数据流：从客户端发起带版本标识的请求，经过网关路由到对应服务版本，然后使用匹配的 Pydantic 模型进行验证和序列化。  
  - 兼容处理：适配层在接收旧版本数据时进行字段映射或注入默认值，在返回时也生成旧版本期望的格式。  
  - 指标：衡量旧版兼容成功率、接口可用性、模式变更前后的错误率以及升级回滚次数等。  
  - 风险：模式演进可能导致旧客户端解析失败；需要严格管理版本间的差异和适配逻辑；多版本维护增加测试和部署复杂度。  
architecture_points:
  - API 网关或路由层识别版本号，并将请求分发到相应的服务。  
  - 后端服务维护多个版本的模型代码（或配置），每个版本使用自己的 Pydantic 定义和 JSON Schema。  
  - 兼容适配层：在服务端对旧版本字段进行映射/填充，保证旧客户端能理解新响应。  
data_flow:
  - 客户端请求（含版本信息）→ 通过网关路由→ 对应版本服务验证输入/生成输出→ 输出通过适配器转化为客户端版本格式→ 返回给客户端。  
evaluation_metrics:
  - 向后兼容性成功率（旧客户端请求成功率）、版本切换错误率、发布后回滚次数、验证失败率等。  
risk_points:
  - 新旧版本字段冲突（如字段重命名、删除），可能导致解析失败。  
  - 维护多版本代码和 Schema 增加复杂度，易出错。  
  - 部署过程中的版本同步问题，需严格测试和回退机制。

--- question ---

question_id: 27
module: Agent 基础与架构
type: system_design
difficulty: 5
knowledge_points:
  - 性能
  - 可扩展性
stem: |
  设计一个高性能的结构化输出验证系统，可扩展以支持大量并发请求。
reference_answer: |
  **核心模块**：请求入口层（负载均衡）、模型推理层、验证服务层（可能分布式部署）、缓存层和监控层。  
  **数据流**：用户请求→负载均衡分发→调用 LLM 生成结构化输出→将输出送入多个验证服务实例进行并行验证（使用异步或线程池）→验证成功后返回响应；失败则记录并处理。  
  **评估指标**：系统吞吐量（TPS）、响应延迟（P50/P95）、模型和验证模块的CPU/内存利用率、验证错误率等。  
  **风险点**：验证服务可能成为瓶颈，需水平扩展；Pydantic 模型加载或编译开销影响性能；缓存命中率低会增加延迟；模型输出偶尔无效导致额外负载；安全风险包括对恶意输入的验证攻击等。  
answer_keywords:
  - 负载均衡
  - 并行 验证
  - 缓存 模式
  - 性能 指标
  - 并发 伸缩
scoring_points:
  - 架构：使用多实例部署验证服务，通过负载均衡或消息队列分配请求，确保可扩展。  
  - 数据流：描述从请求到 LLM 输出再到并行验证的整个流程，包括错误重试机制。  
  - 性能优化：可以缓存模型模式、复用 Pydantic 实例、使用异步或多线程验证等手段提升吞吐量。  
  - 评估指标：TPS、平均/延迟时间、成功率、资源使用率等。  
  - 风险：单点瓶颈、验证延迟过高、模型不稳定输出、缓存失效和安全漏洞。  
architecture_points:
  - 前端负载均衡器、LLM 推理集群、验证服务集群（可扩展）、缓存（存放常用 JSON 模式）和监控系统。  
  - 验证层采用多实例并行处理，使用协程或多线程以降低响应时间。  
data_flow:
  - 客户端请求→模型推理服务生成JSON→验证服务（多个实例）并发验证→返回验证结果→响应客户端。  
evaluation_metrics:
  - 系统吞吐量 (requests per second)、平均响应时间(P50/P95)、验证成功率、错误率、资源利用率(CPU/内存)。  
risk_points:
  - 验证层成为瓶颈，需横向扩展或优化算法。  
  - 模式缓存和模型热启动不当会增加延迟。  
  - LLM 输出质量不稳定导致额外负载。  
  - 恶意输入或边界情况可能绕过验证规则。

--- question ---

question_id: 28
module: Agent 基础与架构
type: coding_or_pseudocode
difficulty: 3
knowledge_points:
  - JSON Schema
  - 验证
stem: |
  编写一个函数 `validate_json(schema: dict, data: dict)` 的 Python 风格伪代码，用于使用给定的 JSON Schema 验证数据。  
reference_answer: |
  ```python
  def validate_json(schema: dict, data: dict) -> bool:
      # 验证必需字段
      required_fields = schema.get("required", [])
      for field in required_fields:
          if field not in data:
              return False
      # 验证属性类型和嵌套
      properties = schema.get("properties", {})
      for key, subschema in properties.items():
          if key not in data:
              continue
          value = data[key]
          expected_type = subschema.get("type")
          if expected_type == "integer":
              if not isinstance(value, int):
                  return False
          elif expected_type == "string":
              if not isinstance(value, str):
                  return False
          elif expected_type == "object":
              # 递归验证嵌套对象
              if not validate_json(subschema, value):
                  return False
          elif expected_type == "array":
              if not isinstance(value, list):
                  return False
              item_schema = subschema.get("items", {})
              for item in value:
                  # 简单示例：假设数组元素为整数
                  if not isinstance(item, int):
                      return False
          # 可扩展处理其他类型和约束...
      return True
  ```
  核心思路是检查必需字段、根据 `type` 验证类型、并对嵌套对象递归调用验证函数。
answer_keywords:
  - 递归 验证
  - 必需 字段
  - 类型检查
  - 返回 True/False
scoring_points:
  - 遍历 `required` 列表检查必需字段是否存在，缺失则返回 False。  
  - 遍历 `properties` 字段，对每个键检查类型是否与 `type` 约束匹配。  
  - 对于嵌套的对象类型，递归调用 `validate_json` 进行验证。  
  - 对于数组类型，验证每个元素是否符合 `items` 定义的类型。  
  - 只要所有检查都通过，则返回 True，否则返回 False。
pseudocode: |
  ```python
  def validate_json(schema: dict, data: dict) -> bool:
      # 验证必需字段
      required_fields = schema.get("required", [])
      for field in required_fields:
          if field not in data:
              return False
      # 验证属性类型
      for key, subschema in schema.get("properties", {}).items():
          if key not in data:
              continue
          value = data[key]
          expected_type = subschema.get("type")
          if expected_type == "integer":
              if not isinstance(value, int):
                  return False
          elif expected_type == "string":
              if not isinstance(value, str):
                  return False
          elif expected_type == "object":
              if not validate_json(subschema, value):
                  return False
          elif expected_type == "array":
              if not isinstance(value, list):
                  return False
              for item in value:
                  # 仅验证整数元素（示例）
                  if not isinstance(item, int):
                      return False
      return True
  ```
complexity:
  time: O(n)  # n 为数据字段总数，递归验证会增加级数  
  space:
    space: O(d)  # d 为嵌套深度，递归调用使用的栈空间
test_cases:
  - input:
      schema: {"type": "object", "properties": {"age": {"type": "integer"}}, "required": ["age"]}
      data: {"age": 30}
    expected: True
  - input:
      schema: {"type": "object", "properties": {"age": {"type": "integer"}}}
      data: {"age": "30"}
    expected: False

--- question ---

question_id: 29
module: Agent 基础与架构
type: coding_or_pseudocode
difficulty: 2
knowledge_points:
  - Pydantic
  - JSON Schema
stem: |
  编写一个函数 `model_to_json_schema(model: BaseModel)` 的 Python 风格伪代码，用于生成 Pydantic 模型的 JSON Schema。
reference_answer: |
  ```python
  def model_to_json_schema(model: BaseModel) -> dict:
      # 调用 Pydantic 内部方法生成 JSON Schema 字典
      schema_dict = model.model_json_schema()
      return schema_dict
  ```
  核心思路是利用 Pydantic 提供的 `model_json_schema()` 方法，直接生成并返回 JSON Schema。
answer_keywords:
  - BaseModel
  - model_json_schema
  - 字典 返回
scoring_points:
  - 使用 Pydantic 的内置方法 `model_json_schema()` 直接获取 JSON Schema。  
  - 将生成的模式（dict）作为函数返回值。  
  - 说明该方法可自动包含模型字段、嵌套模型等信息。
pseudocode: |
  ```python
  def model_to_json_schema(model: BaseModel) -> dict:
      # 使用 Pydantic 生成 JSON Schema
      return model.model_json_schema()
  ```
complexity:
  time: O(n)  # n 为模型字段数量
  space:
    space: O(n)  # 返回的模式大小与字段数量正相关
test_cases:
  - input:
      model: BaseModel 定义: `class A(BaseModel): x: int`
    expected:
      {"properties": {"x": {"type": "integer"}}, "$defs": {}, "type": "object"}
  - input:
      model: BaseModel 定义: `class B(BaseModel): name: str = Field(..., title="Name")`
    expected:
      模式包含 "title": "Name" 等信息并正确描述字段。